# NPM Configuration for Production Deployment

# Skip optional dependencies to reduce installation time and size
optional=false

# Disable audit during install to speed up deployment
audit=false

# Disable fund messages to reduce noise
fund=false

# Use cache to speed up repeated installs
cache=/tmp/.npm

# Prefer offline if cache is available
prefer-offline=true
