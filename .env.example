DATABASE_URL=postgresql://user:password@localhost:5432/tubestar
PORT=5000
NODE_ENV=development

# In production, set ALLOWED_ORIGINS to a comma-separated list of allowed CORS origins
# Example: ALLOWED_ORIGINS=https://example.com,https://www.example.com
# If not set in production, only same-origin requests will be allowed
ALLOWED_ORIGINS=

# YouTube OAuth Configuration (Optional)
# Required for connecting user's YouTube account
# To set up:
# 1. Go to Google Cloud Console (https://console.cloud.google.com)
# 2. Create a new project or select existing one
# 3. Enable YouTube Data API v3
# 4. Create OAuth 2.0 credentials (Web application type)
# 5. Add authorized redirect URIs (e.g., http://localhost:5000/api/auth/youtube/callback)
# 6. Copy Client ID and Client Secret below
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=
YOUTUBE_REDIRECT_URI=http://localhost:5000/api/auth/youtube/callback

# AI Configuration (Optional)
# Set this to enable AI-powered features using Google Gemini
# Get your free API key at: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=
